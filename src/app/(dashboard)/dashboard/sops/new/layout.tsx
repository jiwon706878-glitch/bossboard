import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create SOP",
};

export default function NewSOPLayout({ children }: { children: React.ReactNode }) {
  return children;
}
