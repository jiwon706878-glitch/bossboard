import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Onboarding Paths",
};

export default function OnboardingPathsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
